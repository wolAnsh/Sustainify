<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scheduled Pickups</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
            margin: 0;
            padding: 0;
        }
        h1 {
            text-align: center;
            margin-top: 20px;
            color: white; /* Change text color to white for contrast */
            font-size: 40px; /* Header font size */
            background-color: #495057; /* Background color for the header */
            padding: 20px; /* Padding around the text */
            border: 3px solid #343a40; /* Dark border */
            border-radius: 10px; /* Rounded corners for the border */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Shadow effect for the header */
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .pickup-card {
            background-image: url('/assets/img/ScheduledPickupBackground.avif'); /* Replace with your actual image path */
            background-size: cover;
            background-position: center;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            color: black;
            padding: 20px;
            margin-bottom: 20px;
            transition: transform 0.3s;
        }
        .pickup-card:hover {
            transform: scale(1.02);
        }
        .pickup-card h2 {
            margin: 10px 0;
        }
        .no-pickups {
            text-align: center;
            font-size: 1.5em;
            color: #dc3545;
        }
        .schedule-button {
            display: block;
            width: 100%;
            max-width: 200px; /* Maximum width for the button */
            margin: 20px auto; /* Centering the button */
            padding: 15px;
            background-color: black; /* Button background color */
            color: white; /* Button text color */
            font-size: 1.2em;
            font-weight: bold;
            text-align: center;
            border-radius: 5px; /* Rounded corners */
            text-decoration: none; /* No underline */
            transition: background-color 0.3s, transform 0.3s;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Shadow effect */
        }
        .schedule-button:hover {
            background-color: rgb(36, 36, 36); /* Darker color on hover */
            transform: scale(1.05); /* Slight enlargement on hover */
        }

        /* Footer styles */
        footer {
            width: 100%;
            bottom: 0;
            background-color: black;
            height: 250px;
            border-top-left-radius: 125px;
            padding: 100px 40px 30px;
        }
        footer p {
            color: white;
            font-size: 18px;
            cursor: pointer;
            justify-content: space-evenly;
        }
        .footernav {
            margin: 30px 0;
        }
        .footernav ul {
            display: flex;
            justify-content: center;
            list-style-type: none;
        }
        .footernav ul li a {
            color: white;
            margin: 20px;
            text-decoration: none;
            font-size: 1.3em;
            opacity: 0.7;
            transition: 0.5s;
        }
        .footernav ul li a:hover {
            opacity: 1;
        }
        .footerbottom {
            padding: 20px;
            text-align: center;
        }
        .footerbottom p {
            color: white;
        }
        .social_icons {
            display: flex;
            justify-content: center;
        }
        .social_icons a {
            text-decoration: none;
            padding: 10px;
            background-color: white;
            margin: 10px;
            border-radius: 50%;
        }
        .social_icons a i {
            font-size: 2em;
            color: black;
            opacity: 0.9;
        }
        .social_icons a:hover {
            background-color: #111;
            transition: 0.5s;
        }
        .social_icons a:hover i {
            color: white;
            transition: 0.5s;
        }

        /* Navigation bar styles */
        nav {
            display: flex; /* Use flexbox for layout */
            justify-content: space-between; /* Space between logo and nav links */
            align-items: center; /* Center items vertically */
            height: 75px;
            background-color: black; /* Ensure nav has the same background as the footer */
            padding: 0 50px; /* Add padding to the sides */
        }
        nav p {
            color: white; /* Ensure text is white */
            font-size: 35px;
            margin: 0; /* Reset margin for better alignment */
        }
        .nav {
            display: flex; /* Use flex for nav links */
        }
        nav li {
            display: inline-block;
            list-style: none;
            margin: 0; /* Reset margin for better alignment */
            padding: 0; /* Reset padding for better alignment */
        }
        nav li a {
            font-size: 18px;
            text-transform: uppercase;
            padding: 0 30px; /* Add horizontal padding */
            color: white;
            text-decoration: none;
        }
        nav li a:hover {
            color: green;
        }
        .cancel-button {
                display: block;
                width: 100%;
                max-width: 200px;
                margin: 20px auto;
                padding: 15px;
                background-color: rgb(0, 0, 0);
                color: white;
                font-size: 1.2em;
                font-weight: bold;
                text-align: center;
                border-radius: 5px;
                text-decoration: none;
                transition: background-color 0.3s, transform 0.3s;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            }

            .cancel-button:hover {
                background-color: darkred;
                transform: scale(1.05);
            }
        @media (max-width: 700px) {
            .footernav ul {
                flex-direction: column;
                align-items: center; /* Centering links vertically */
            }
            .footernav ul li {
                text-align: center;
                margin: 10px;
            }
            .social_icons a {
                padding: 8px;
                margin: 4px;
            }
            .footernav ul li a {
                width: 100%; /* Ensure links take full width */
            }
        }
    </style>
</head>
<body>
    <nav>
        <div class="container">
            <!-- <p style="font-size: 30px; display: inline-block;"><b><a style="text-decoration: none; color: inherit;" href="/index">Sustainify</a></b></p> -->
            <div class="nav">
                <ul>
                    <li><a href="/index">Home</a></li>
                    <li><a href="/ScrapRates">Scrap Rates</a></li>
                    <li><a href="/PriceCalculator">Price Calculator</a></li>
                   
                </ul>
            </div>
        </div>
    </nav>

    <h1><b>Scheduled Pickups</b></h1>
    <!-- <a href="/SchedulePickup" class="schedule-button">Schedule a Pickup</a> -->
    <form action="/SchedulePickup" method="POST">
        <button type="submit" class="schedule-button">Schedule a Pickup</button>
    </form>
    <div class="container">
        <% if (pickups.length > 0) { %>
            <% pickups.forEach(function(val) { %>
                <div class="pickup-card">
                    <h2>PickupID: <%= val._id %></h2>
                    <p><b>Date of Pickup:</b> <%= val.date %></p>
                    <p><b>Scheduled at:</b> <%= val.time %></p>
                    <p><b>Address:</b> <%= val.address %></p>
                    <p><b>Pincode:</b> <%= val.pincode %></p>
                    <p><b>Weight to pickup:</b> <%= val.weight %></p>
                    <p><b>Remarks:</b> <%= val.remarks %></p>
                    <a href="/cancelPickup/<%= val._id %>" class="cancel-button">Cancel Pickup</a>
                </div>
            <% }) %>
        <% } else { %>
            <h1 class="no-pickups">No Pickups Yet!!!</h1>
        <% } %>
        
    </div>

    <footer>
        <div class="footernav" id="about_us">
            <div class="social_icons">
                <a href="https://www.facebook.com/iiitall/"><i class="fa-brands fa-facebook"></i></a>
                <a href="https://www.instagram.com/iiit.allahabad/"><i class="fa-brands fa-instagram"></i></a>
                <a href="https://twitter.com/iiita_official?lang=en"><i class="fa-brands fa-twitter"></i></a>
            </div>
            <div class="footernav">
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/login">Login</a></li>
                    <li><a href="https://www.iiita.ac.in/" target="_blank">About IIITA</a></li>
                    <!-- <li><a href="">Contact Us</a></li> -->
                </ul>
            </div>
        </div>
        <div class="footerbottom">
            <p>Copyright &copy; 2023</p>
        </div>
    </footer>
</body>
</html>